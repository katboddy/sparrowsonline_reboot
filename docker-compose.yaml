services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app   # live reload without rebuild
    env_file:
      - .env
    restart: unless-stopped
